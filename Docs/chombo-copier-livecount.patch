diff --git a/lib/src/BoxTools/Copier.H b/lib/src/BoxTools/Copier.H
index b200ce5..f4f51ac 100644
--- a/lib/src/BoxTools/Copier.H
+++ b/lib/src/BoxTools/Copier.H
@@ -155,7 +155,16 @@ public:
 
   ///null constructor, copy constructor and operator= can be compiler defined.
   Copier():m_isDefined(false)
-  {}
+  {
+    s_liveCopiers++;
+  }
+
+  /**
+     DIAGNOSTIC. Copiers constructed minus Copiers destroyed. A Copier owns its motion plan --
+     MotionItems plus the per-rank index vectors -- so a live count that climbs from one regrid to
+     the next means those plans are being retained, and the layouts they refer to with them.
+  */
+  static long long s_liveCopiers;
 
   Copier(const Copier& a_rhs);
 
diff --git a/lib/src/BoxTools/Copier.cpp b/lib/src/BoxTools/Copier.cpp
index beeb528..2247818 100644
--- a/lib/src/BoxTools/Copier.cpp
+++ b/lib/src/BoxTools/Copier.cpp
@@ -44,11 +44,14 @@ void CopierBuffer::clear()
   m_ncomps = 0;
 }
 
+long long Copier::s_liveCopiers = 0;
+
 Copier::Copier(const DisjointBoxLayout& a_level,
                const BoxLayout& a_dest,
                bool a_exchange,
                IntVect a_shift)
 {
+  s_liveCopiers++;
   const ProblemDomain& domain = a_level.physDomain();
   define(a_level, a_dest, domain, IntVect::Zero, a_exchange, a_shift);
 }
@@ -59,6 +62,7 @@ Copier::Copier(const DisjointBoxLayout& a_level,
                bool a_exchange,
                IntVect a_shift)
 {
+  s_liveCopiers++;
   define(a_level, a_dest, a_domain, IntVect::Zero, a_exchange, a_shift);
 }
 
@@ -68,6 +72,7 @@ Copier::Copier(const DisjointBoxLayout& a_level,
                bool a_exchange,
                IntVect a_shift)
 {
+  s_liveCopiers++;
   const ProblemDomain domain = a_level.physDomain();
   define(a_level, a_dest, domain, a_destGhost, a_exchange, a_shift);
 }
@@ -79,6 +84,7 @@ Copier::Copier(const DisjointBoxLayout& a_level,
                bool a_exchange,
                IntVect a_shift)
 {
+  s_liveCopiers++;
   define(a_level, a_dest, a_domain, a_destGhost, a_exchange, a_shift);
 }
 
@@ -106,6 +112,7 @@ void Copier::clear()
 
 Copier::Copier(const Copier& a_rhs)
 {
+  s_liveCopiers++;
   *this = a_rhs;
 }
 
@@ -1260,6 +1267,7 @@ ostream& operator<< (ostream& os, const Copier& copier)
 Copier::~Copier()
 {
   CH_TIME("~Copier");
+  s_liveCopiers--;
   clear();
 }
 
diff --git a/lib/src/EBTools/EBISLevel.H b/lib/src/EBTools/EBISLevel.H
index 70d0e00..8f2073d 100644
--- a/lib/src/EBTools/EBISLevel.H
+++ b/lib/src/EBTools/EBISLevel.H
@@ -202,6 +202,31 @@ public:
 
   void printGraphSummary(char const * a_prefix);
 
+  ///
+  /**
+     DIAGNOSTIC. Number of EBISLayouts held in this level's cache. Each entry owns a ghosted copy of
+     the graph and the EBData for its layout, so this counts retained geometry. Keys are ordered by
+     the address of the shared layout implementation, so a regrid can never hit this cache -- every
+     one builds a fresh entry.
+  */
+  int getCacheSize() const { return m_cache.size(); }
+
+  ///
+  /**
+     DIAGNOSTIC. Cache entries some other object still references. refreshCache() evicts only at
+     refcount one, so a pinned count that climbs regrid-over-regrid means a holder is outliving its
+     grids and the entry can never be reclaimed.
+  */
+  int getCachePinned() const;
+
+  ///
+  /** DIAGNOSTIC. Cumulative cache hits. */
+  int getCacheHits() const { return m_cacheHits; }
+
+  ///
+  /** DIAGNOSTIC. Cumulative cache misses; each one builds a new graph and EBData copy. */
+  int getCacheMisses() const { return m_cacheMisses; }
+
   int m_phase;
 
   static bool s_recursive;
diff --git a/lib/src/EBTools/EBISLevel.cpp b/lib/src/EBTools/EBISLevel.cpp
index af2c3b8..ff73d32 100644
--- a/lib/src/EBTools/EBISLevel.cpp
+++ b/lib/src/EBTools/EBISLevel.cpp
@@ -1184,6 +1184,23 @@ void EBISLevel::dumpCache() const
     }
 
 } 
+int EBISLevel::getCachePinned() const
+{
+  int pinned = 0;
+
+  dmap::const_iterator d = m_cache.begin();
+  while (d != m_cache.end())
+    {
+      if(d->second.refCount() > 1)
+        {
+          pinned++;
+        }
+      d++;
+    }
+
+  return pinned;
+}
+
 void EBISLevel::refreshCache() const
 {
   dmap::iterator d = m_cache.begin();
diff --git a/lib/src/EBTools/EBIndexSpace.H b/lib/src/EBTools/EBIndexSpace.H
index 028b8e6..39a941a 100644
--- a/lib/src/EBTools/EBIndexSpace.H
+++ b/lib/src/EBTools/EBIndexSpace.H
@@ -125,6 +125,14 @@ public:
   ///
   int numLevels() const;
 
+  ///
+  /**
+     DIAGNOSTIC. Report per-level EBISLayout cache occupancy. An EBIndexSpace is built once from the
+     geometry and is never cleared by a regrid, so anything retained here accumulates for the life of
+     the run.
+  */
+  void reportLayoutCache(std::ostream& a_os, const char* a_prefix) const;
+
   ///
   /**
      Get the number of vofs over the entire domain.
diff --git a/lib/src/EBTools/EBIndexSpace.cpp b/lib/src/EBTools/EBIndexSpace.cpp
index 755b9ab..933d6b6 100644
--- a/lib/src/EBTools/EBIndexSpace.cpp
+++ b/lib/src/EBTools/EBIndexSpace.cpp
@@ -705,6 +705,31 @@ int EBIndexSpace::numLevels() const
   return m_nlevels;
 }
 
+void EBIndexSpace::reportLayoutCache(std::ostream& a_os, const char* a_prefix) const
+{
+  int totalSize   = 0;
+  int totalPinned = 0;
+
+  for (int ilev = 0; ilev < m_nlevels; ilev++)
+    {
+      if (m_ebisLevel[ilev] == NULL) continue;
+
+      const int size   = m_ebisLevel[ilev]->getCacheSize();
+      const int pinned = m_ebisLevel[ilev]->getCachePinned();
+
+      totalSize   += size;
+      totalPinned += pinned;
+
+      a_os << a_prefix << " EBIScache lvl " << ilev
+           << " entries = " << size
+           << " pinned = "  << pinned
+           << " hits = "    << m_ebisLevel[ilev]->getCacheHits()
+           << " misses = "  << m_ebisLevel[ilev]->getCacheMisses() << "\n";
+    }
+
+  a_os << a_prefix << " EBIScache TOTAL entries = " << totalSize << " pinned = " << totalPinned << "\n";
+}
+
 void EBIndexSpace::clear()
 {
   for (int ilev = 0; ilev < m_ebisLevel.size(); ilev++)
