[geometry/aerosol2d]
  # Subfolder where this test is located
  directory     = geometry/aerosol

  # Problem dimension
  dim           = 2

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression2d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = aerosol2d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = aerosol2d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/aerosol3d]
  # Subfolder where this test is located
  directory     = geometry/aerosol

  # Problem dimension
  dim           = 3

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression3d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = aerosol3d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = aerosol3d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/coaxial_cable2d]
  # Subfolder where this test is located
  directory     = geometry/coaxial_cable

  # Problem dimension
  dim           = 2

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression2d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = coaxial_cable2d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = coaxial_cable2d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/coaxial_cable3d]
  # Subfolder where this test is located
  directory     = geometry/coaxial_cable

  # Problem dimension
  dim           = 3

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression3d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = coaxial_cable3d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = coaxial_cable3d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/double_rod2d]
  # Subfolder where this test is located
  directory     = geometry/double_rod

  # Problem dimension
  dim           = 2

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression2d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = double_rod2d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = double_rod2d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/double_rod3d]
  # Subfolder where this test is located
  directory     = geometry/double_rod

  # Problem dimension
  dim           = 3

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression3d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = double_rod3d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = double_rod3d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/electrode_array2d]
  # Subfolder where this test is located
  directory     = geometry/electrode_array

  # Problem dimension
  dim           = 2

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression2d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = electrode_array2d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = electrode_array2d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/electrode_array3d]
  # Subfolder where this test is located
  directory     = geometry/electrode_array

  # Problem dimension
  dim           = 3

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression3d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = electrode_array3d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = electrode_array3d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/mechanical_shaft3d]
  # Subfolder where this test is located
  directory     = geometry/mechanical_shaft

  # Problem dimension
  dim           = 3

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression3d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = mechshaft3d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = mechshaft3d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/porsche]
  # Subfolder where this test is located
  directory     = geometry/porsche

  # Problem dimension
  dim           = 3

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression3d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = porsche

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = porsche_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/regular_geometry2d]
  # Subfolder where this test is located
  directory     = geometry/regular_geometry

  # Problem dimension
  dim           = 2

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression2d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = regular_geometry2d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = regular_geometry2d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/regular_geometry3d]
  # Subfolder where this test is located
  directory     = geometry/regular_geometry

  # Problem dimension
  dim           = 3

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression3d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = regular_geometry3d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = regular_geometry3d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/rod_box2d]
  # Subfolder where this test is located
  directory     = geometry/rod_box

  # Problem dimension
  dim           = 2

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression2d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = rod_box2d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = rod_box2d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/rod_box3d]
  # Subfolder where this test is located
  directory     = geometry/rod_box

  # Problem dimension
  dim           = 3

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression3d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = rod_box3d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = rod_box3d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/rod_perlin_box2d]
  # Subfolder where this test is located
  directory     = geometry/rod_perlin_box

  # Problem dimension
  dim           = 2

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression2d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = rod_perlin_box2d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = rod_perlin_box2d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/rod_perlin_box3d]
  # Subfolder where this test is located
  directory     = geometry/rod_perlin_box

  # Problem dimension
  dim           = 3

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression3d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = rod_perlin_box3d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = rod_perlin_box3d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/rod_plane2d]
  # Subfolder where this test is located
  directory     = geometry/rod_plane

  # Problem dimension
  dim           = 2

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression2d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = rod_plane2d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = rod_plane2d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/rod_plane3d]
  # Subfolder where this test is located
  directory     = geometry/rod_plane

  # Problem dimension
  dim           = 3

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression3d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = rod_plane3d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = rod_plane3d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/rod_sphere2d]
  # Subfolder where this test is located
  directory     = geometry/rod_sphere

  # Problem dimension
  dim           = 2

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression2d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = rod_sphere2d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = rod_sphere2d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/rod_sphere3d]
  # Subfolder where this test is located
  directory     = geometry/rod_sphere

  # Problem dimension
  dim           = 3

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression3d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = rod_sphere3d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = rod_sphere3d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/rough_rod2d]
  # Subfolder where this test is located
  directory     = geometry/rough_rod

  # Problem dimension
  dim           = 2

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression2d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = rough_rod2d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = rough_rod2d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/rough_rod3d]
  # Subfolder where this test is located
  directory     = geometry/rough_rod

  # Problem dimension
  dim           = 3

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression3d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = rough_rod3d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = rough_rod3d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/rough_sphere2d]
  # Subfolder where this test is located
  directory     = geometry/rough_sphere

  # Problem dimension
  dim           = 2

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression2d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = rough_sphere2d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = rough_sphere2d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/rough_sphere3d]
  # Subfolder where this test is located
  directory     = geometry/rough_sphere

  # Problem dimension
  dim           = 3

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression3d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = rough_sphere3d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = rough_sphere3d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/vessel2d]
  # Subfolder where this test is located
  directory     = geometry/vessel

  # Problem dimension
  dim           = 2

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression2d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = vessel2d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = vessel2d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/vessel3d]
  # Subfolder where this test is located
  directory     = geometry/vessel

  # Problem dimension
  dim           = 3

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression3d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = vessel3d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = vessel3d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/wire_wire2d]
  # Subfolder where this test is located
  directory     = geometry/wire_wire

  # Problem dimension
  dim           = 2

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression2d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = wire_wire2d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = wire_wire2d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0

[geometry/wire_wire3d]
  # Subfolder where this test is located
  directory     = geometry/wire_wire

  # Problem dimension
  dim           = 3

  # Prefix name of the executable. The executable is named according to the PlasmaC
  # configuration string. E.g. this executable will be named main2d.<BunchOfOptions>.ex
  exec          = main

  # Regression input file name prefix.
  # Your actual filename should be appended with dimension and .inputs.
  # E.g. for this test the filename is regression2d.inputs in 2d, and regression3d.inputs in 3d
  input         = regression3d.inputs

  # Output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named poisson.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  output        = wire_wire3d

  # Number of processes to use when running the regression test
  num_procs     = 6

  # Benchmark output filenames. The files are named using the PlasmaC driver configuration string.
  # E.g. the output files will be named benchmark.stepXXXXXXX.2d.hdf5
  # and they are located in [directory]/plt
  benchmark     = wire_wire3d_benchmark

  # Number of time steps to run for this test. 
  nsteps        = 0

  # Plot interval for this test. 
  plot_interval = 10

  # Which timestep to restart from. Note that benchmark files always start from the first time step
  restart       = 0
