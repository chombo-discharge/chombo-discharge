# ====================================================================================================
# FIELD_STEPPER CLASS OPTIONS
# ====================================================================================================
field_stepper.verbosity    = -1              # Verbosity
field_stepper.realm        = primal          # Primal realm
field_stepper.load_balance = false           # Load balance or not.
field_stepper.box_sorting  = morton          # If you load balance you can redo the box sorting. 
field_stepper.init_rho     = 0.0             # Space charge density
field_stepper.init_sigma   = 0.0             # Surface charge density